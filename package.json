{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "prepare": "lerna run prepare",
    "clean": "rimraf packages/**/lib"
  },
  "devDependencies": {
    "lerna": "^3.0.0-rc.0"
  },
  "dependencies": {},
  "workspaces": [
    "packages/*",
    "services/*"
  ]
}
